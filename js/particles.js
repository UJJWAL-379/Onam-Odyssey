export class ParticleSystem{constructor(max=240){this.max=max;this.items=[]}burst(x,y,n=12,color='#ffe37a',speed=180){for(let i=0;i<n&&this.items.length<this.max;i++){const a=Math.random()*Math.PI*2,s=Math.random()*speed;this.items.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.35+Math.random()*.55,size:2+Math.random()*4,color})}}text(x,y,text){this.items.push({x,y,vx:0,vy:-45,life:1,size:16,text,color:'#fff1a1'})}update(dt){for(const p of this.items){p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=180*dt;p.life-=dt}this.items=this.items.filter(p=>p.life>0)}draw(c){for(const p of this.items){c.globalAlpha=Math.max(0,p.life);if(p.text){c.font='900 '+p.size+'px system-ui';c.fillStyle=p.color;c.fillText(p.text,p.x,p.y)}else{c.fillStyle=p.color;c.fillRect(p.x,p.y,p.size,p.size)}}c.globalAlpha=1}}